# Option Chaser Brief v4  
## Artifact Parity、正式產品化與 Windows 一鍵啟動

---

# 1. 本版本核心目標

目前 Option Chaser 的計算引擎、多劇本工作區與資料保存地基可以保留，不得推倒重做。

但目前實際 App 與先前提供的 Claude Artifact 存在巨大落差：

```text
https://claude.ai/code/artifact/bca2c75f-453c-40f0-9c56-d726b17b3d69
```

Artifact 呈現的是現代、清楚、漂亮的正式交易產品；實際 Repo 跑起來則仍像 Streamlit 工程後台。

因此本版本唯一最高優先目標是：

> **把 Artifact 中展示的視覺、資訊層級與操作流程，真正落地到可執行的 Option Chaser。**

本版本不是再製作一張新 Mockup，也不是只做「精神相似」的介面。

必須交付：

```text
Artifact 所展示的產品
≈
使用者實際從 Repo 啟動後看到的產品
```

---

# 2. 本輪工作方式

本輪先完成：

1. 檢查目前 Repo 與最新 `master`。
2. 實際開啟 Artifact。
3. 實際啟動目前 App。
4. 建立「Artifact vs 實際 App」逐頁差異表。
5. 產生本版本 Design Spec。
6. 產生實作 Plan。
7. 交由獨立 Reviewer 審查。

在使用者確認 Spec 與 Plan 前：

- 不得直接施工。
- 不得自行改變 Artifact 的核心視覺方向。
- 不得把功能存在視為視覺完成。
- 不得再用另一張新草圖替代實際產品施工。

---

# 3. Artifact 是正式視覺基準

本版本將以下 Artifact 視為正式 UI／UX Source of Truth：

```text
https://claude.ai/code/artifact/bca2c75f-453c-40f0-9c56-d726b17b3d69
```

使用者喜歡其中的：

- 深色專業交易介面
- 現代、高級、乾淨的視覺
- 卡片式資訊結構
- 清楚的層級
- 數字與標籤的對比
- 劇本與候選之間的關係表達
- 不像工程後台的正式產品感

實作時應盡量還原：

- 整體版面
- 導覽
- 卡片
- 間距
- 字級
- 色彩
- 狀態標章
- 數字排列
- 按鈕位置
- 比較區
- 詳情區
- 桌機與手機結構

不得只換成深色背景，再保留目前雜亂的 `st.columns` 排列。

---

# 4. 目前已確認的產品問題

## 4.1 實際 App 與 Artifact 不一致

目前實際介面主要由 Streamlit 預設元件直接排列：

- 多欄資料
- 多個按鈕
- 多個 Checkbox
- 多個 Expander
- 狀態、刪除與主要分析混在一起

結果像管理後台，而不是 Artifact 中的正式交易產品。

本版本必須處理的是整體資訊架構，不只是 CSS 美化。

---

## 4.2 導覽出現 `app`

目前 Streamlit 導覽會顯示一個叫：

```text
app
```

的頁面。

這不是合理的正式產品名稱。

正式導覽應調整為：

```text
戰情總覽
劇本工作區
快速試算
使用說明
```

不得再讓使用者看到：

```text
app
```

---

## 4.3 兩個地方都能新增相似劇本

目前：

### 主 App 頁

可以輸入：

- 標的
- 目標價
- 目標日期
- 策略

### 劇本工作區

也可以輸入：

- 標的
- 目標價
- 目標日期
- 方向
- 策略
- 備註

工程上兩者分別是：

```text
主 App：一次性分析
劇本工作區：持久化劇本
```

但產品沒有說明差別，因此使用者不知道應該在哪裡操作。

本版本應重新定義：

### 正式流程

```text
劇本工作區
→ 建立劇本
→ 執行分析
→ 比較候選
```

### 快速試算

```text
快速試算（不保存）
```

必須明確標示：

> 本頁結果不會自動加入劇本工作區。

可提供：

```text
保存為劇本
```

讓使用者將試算結果轉入工作區。

不得保留兩套外觀相同、定位不清的新增流程。

---

# 5. 候選價格必須顯示

目前引擎與 Result JSON 已經包含：

- 每腿 Bid
- 每腿 Ask
- Mid Cost
- Natural Cost
- Spread Net Mid Debit
- 每張／每組成本
- 最大損失
- 最大獲利
- Breakeven

但實際主要 GUI 沒有清楚顯示。

這是重大產品問題。

Option 比較不可以只顯示報酬率，卻不顯示交易成本。

---

## 5.1 Long Call／Long Put 顯示格式

候選卡至少顯示：

```text
Long Call
履約價：93
到期日：2028-01-21

Bid：$1.45
Mid：$1.50
Ask：$1.55

Mid 每張：約 $150
Natural 每張：約 $155
最大損失：約 $150
Breakeven：$94.50
```

金額必須明確區分：

- 每股價格
- 每張合約金額（×100）

---

## 5.2 Spread 顯示格式

候選卡至少顯示：

```text
Bull Call Spread
買入：93 Call
賣出：100 Call
到期日：2028-01-21

Net Mid Debit：$0.66／股
每組成本：約 $66
Natural Debit：$0.80／股
Natural 每組：約 $80

最大損失：約 $66
最大獲利：約 $634
Breakeven：$93.66
獲利封頂價：$100
```

必須明確顯示：

- 買腿
- 賣腿
- Net Debit
- 每組成本
- 最大損失
- 最大獲利
- 封頂價位

---

## 5.3 候選比較表

每個候選至少包含：

| 欄位 | 要求 |
|---|---|
| 策略 | Long Call／BCS 等 |
| 結構 | K=93 或買93／賣100 |
| 到期日 | 完整日期 |
| Bid／Mid／Ask | 單腿 |
| Net Mid／Natural Debit | Spread |
| 每張／每組成本 | 必須顯示 |
| 最大損失 | Debit 成本 |
| 最大獲利 | Spread 必須顯示 |
| Breakeven | 必須顯示 |
| 劇本報酬 | 目前基準 |
| 容錯資訊 | 目前已有指標 |
| 成交摩擦 | 獨立顯示 |
| 資料品質 | 正常／警告 |

---

# 6. Spread Heatmap 必須解釋獲利封頂

真實使用時，使用者看到：

```text
TLT 105～115 的到期收益率都約 3233%
```

容易以為計算錯誤。

實際原因可能是 Bull Call Spread 已超過賣出腿履約價，因此達到最大獲利。

本版本必須在 Spread Heatmap 上明確顯示：

```text
Bull Call Spread：買 100／賣 101
Net Mid 成本：$0.03／股
每組成本：約 $3
最大獲利：約 $97
股價 ≥ $101 後，收益固定於最大獲利
```

Heatmap 中高於賣出腿 Strike 的區域應視覺標示：

```text
最大獲利區
```

或：

```text
收益封頂
```

要求：

1. Long Call 與 Bull Call Spread 的主圖必須明顯不同。
2. Spread 必須顯示封頂價位。
3. Spread 必須顯示最大獲利。
4. 同值平台必須有文字解釋。
5. 不可只顯示大量相同綠色百分比。

---

# 7. 正式頁面架構

## 7.1 戰情總覽

作為正式首頁。

本版本可先顯示：

- Active 劇本數
- 尚未分析劇本數
- 資料異常劇本數
- 最近分析時間
- 待確認同標的關係
- 已完成劇本數

目前沒有 Position，因此不得偽造：

- 持倉損益
- 已投入資金
- Portfolio Greeks

---

## 7.2 劇本工作區

正式建立劇本的主要入口。

劇本以 Artifact 風格卡片呈現。

每張卡主要顯示：

- Symbol
- 方向
- 現價
- 目標價
- 目標日期
- 目標漲跌幅
- 狀態
- 最新分析時間
- 資料品質
- 最新推薦候選
- 候選成本
- 劇本報酬
- 關鍵容錯資訊

主要操作：

```text
查看分析
重新分析
選擇候選
```

低頻操作放入：

```text
⋯ 管理
```

包括：

- 標記達成
- 標記失效
- 修改備註
- 關係設定
- 刪除劇本

不得將原因欄、刪除 Checkbox、狀態按鈕長期全部攤在主畫面。

---

## 7.3 快速試算

正式名稱：

```text
快速試算
```

副標題：

> 一次性分析，結果不會自動保存。

操作結束後提供：

```text
保存為劇本
```

快速試算與劇本工作區應共用同一套：

- 輸入元件
- 分析 Service
- 候選卡
- Heatmap
- 結果渲染

不得維護兩套不同結果頁。

---

## 7.4 劇本詳頁

詳頁應是獨立頁面，不應插在整個劇本清單底部。

頁面至少包含：

### 劇本 Header

- Symbol
- 現價
- 目標價
- 目標日期
- Snapshot 時間
- 資料品質
- 重新分析

### 推薦候選

- 策略與腿
- 到期日
- Bid／Mid／Ask
- 每張成本
- 最大損失
- 最大獲利
- Breakeven
- 劇本報酬

### Heatmap

- 價格 × 日期
- 關鍵價位
- 目標日
- 到期日
- Spread 封頂區

### 候選比較

本版本至少保留目前候選比較能力，且所有候選顯示成本。

### 進階資訊

- 劇本情境
- Greeks
- 流動性
- 成交摩擦
- 原始報告

---

# 8. 同標的群組視覺

同一標的不同劇本應參考 Artifact 做成里程碑結構。

例如：

```text
TLT

2028-01
目標 105
   ↓
2028-12
目標 120
```

顯示：

- 劇本狀態
- 日期順序
- 已確認關係
- 尚待確認關係
- 最新分析結果
- 是否共用相同 Snapshot

底層仍維持：

- 確定性規則提出關係
- 使用者手動確認
- 不使用 LLM 自動控制

---

# 9. 資料品質與無報價狀態

真實盤前資料可能出現大量：

```text
Bid = 0
Ask = 0
```

目前容易顯示成：

```text
尚未分析
```

這是不正確的。

必須區分：

```text
尚未分析
分析成功，有可用候選
分析成功，但沒有合格候選
分析成功，但報價資料不足
抓取市場資料失敗
分析執行失敗
使用歷史 Snapshot
使用測試 fallback
```

若分析已執行，但無候選，應顯示：

> 已完成分析，但目前報價資料不足，沒有可用候選。

並可提供：

```text
稍後重試
使用最近有效 Snapshot
查看過濾原因
```

Last Trade fallback 只可作為測試模式，必須明確標示：

```text
Synthetic Test／不可作為成交依據
```

不得進入正式候選推薦。

---

# 10. Windows 一鍵啟動

使用者目前不應每次手動輸入 PowerShell 指令。

本版本必須提供：

```text
啟動 Option Chaser.bat
```

放在專案根目錄，並可建立桌面捷徑。

---

## 10.1 啟動 BAT 的使用體驗

使用者雙擊：

```text
啟動 Option Chaser.bat
```

後應自動：

1. 找到專案根目錄。
2. 切換到正確工作目錄。
3. 檢查 Python 是否存在。
4. 檢查必要套件是否存在。
5. 缺少依賴時提供明確訊息或執行安裝。
6. 啟動 Streamlit。
7. 自動開啟瀏覽器：
   ```text
   http://localhost:8501
   ```
8. 保留命令視窗以顯示執行狀態。
9. 發生錯誤時不得瞬間關閉。
10. 顯示可理解的中文錯誤訊息。

不得要求使用者手動設定：

```powershell
$env:PYTHONPATH = ...
```

正式打包與 Import 必須先修好。

---

## 10.2 BAT 應處理的情況

### Python 不存在

顯示：

```text
找不到 Python。
請先安裝 Python 3.11 以上版本。
```

並暫停視窗。

### 套件未安裝

可選擇：

- 首次啟動自動安裝
- 或提供獨立：
  ```text
  安裝 Option Chaser.bat
  ```

請在 Spec 階段比較兩種方案。

### Port 8501 已被占用

應：

- 偵測既有 Option Chaser 是否已在執行
- 若已執行，直接開啟瀏覽器
- 若是其他程式占用，顯示明確訊息
- 不可靜默失敗

### 啟動錯誤

輸出 Log，並：

```bat
pause
```

避免使用者看不到錯誤。

---

## 10.3 停止方式

至少提供其中一種：

### 方案 A

啟動視窗保持開啟，使用者按：

```text
Ctrl + C
```

停止。

### 方案 B

另提供：

```text
停止 Option Chaser.bat
```

請在 Spec 階段比較安全性與實作複雜度。

不得粗暴終止所有 Python 程序。

---

## 10.4 桌面與專案整潔

使用者的桌面不應長期直接面對整個開發 Repo。

本版本至少應做到：

- 桌面只需放「啟動 Option Chaser」捷徑。
- 使用者不需要打開 Repo 才能啟動。
- 開發檔案保持在專案資料夾內。
- 使用者資料集中於明確資料夾。
- 不得讓 Log、Snapshot、Workspace 檔案散落在桌面。

請評估是否將使用者資料移至：

```text
data/
├── workspace/
├── snapshots/
└── logs/
```

或 Windows 使用者資料目錄。

但不得在沒有 Migration Plan 的情況下直接移動現有資料。

---

# 11. 正式啟動與打包修復

目前正式啟動可能因：

```text
webapp.render
```

未正確納入 Package 而失敗。

必須修復：

```bash
pip install -e ".[gui]"
streamlit run webapp/app.py
```

要求：

- 不使用 PYTHONPATH workaround
- 本機正常啟動
- Multipage 正常
- `webapp.render` 正常
- Docker 正常
- BAT 正常

版本號也必須統一。

---

# 12. 本版本範圍

## 必做

1. 以指定 Artifact 作為正式視覺基準。
2. 建立 Artifact vs 實際 App 差異清單。
3. 重構正式導覽。
4. 移除 `app` 頁名。
5. 區分快速試算與持久化劇本。
6. 劇本工作區成為正式主流程。
7. 實際 App 高度還原 Artifact。
8. 所有候選顯示價格。
9. 顯示每股與每張／每組成本。
10. Spread 顯示最大損失、最大獲利與封頂價。
11. Spread Heatmap 標示最大獲利區。
12. 修正無報價與無候選的狀態文案。
13. 修復正式啟動與 Docker。
14. 提供 `啟動 Option Chaser.bat`。
15. 提供清楚的首次啟動與錯誤處理。
16. 使用真實劇本做 Chromium 驗收。
17. 提供實際桌機與手機截圖。
18. 視覺由使用者核可後才可合併。

---

## 可順手處理，但不能阻礙主目標

- 「佔本金」改為「每口成本占本金」
- 最近有效 Snapshot
- Top 5 顯示
- 候選展開
- 更清楚的資料品質標章

---

## 延後到下一版本

- 韌性排名完整重構
- Scenario Decision 持久化
- 持倉建立
- 追蹤面板
- 倉位配置
- Policy Engine
- 自動事件觸發
- 券商 API
- 自動下單

若實作 Artifact UI 時需要預留介面，可以預留，但不得擴大本版本範圍。

---

# 13. 真實劇本驗收

至少使用：

```text
劇本 1
TLT
2028-01-01
目標價 105

劇本 2
TLT
2028-12-01
目標價 120

劇本 3
ORCL
2027-01-01
目標價 250
```

驗證：

- 三個劇本可建立
- TLT 自動歸組
- TLT 共用 Snapshot
- ORCL 獨立 Snapshot
- 每個候選顯示價格
- 每個候選顯示每張／每組成本
- Spread 顯示封頂
- 無報價時顯示正確狀態
- 快速試算不會偷偷保存
- 保存為劇本正常
- BAT 可從乾淨狀態啟動

---

# 14. 視覺驗收

不得只使用 AppTest 或文字斷言驗收。

必須提供：

1. Artifact 原始參考畫面。
2. 實際 Chromium 桌機截圖。
3. 實際 Chromium 手機截圖。
4. Artifact vs 實際畫面的逐頁差異表。
5. 每項差異的狀態：
   - 已還原
   - 合理調整
   - 尚未完成
6. 未完成部分不得藏起來。
7. 使用者明確確認視覺後才可合併。

不得再用漂亮 Artifact 代替實際 App 截圖。

---

# 15. BAT 驗收

必須在 Windows 實機驗證：

```text
雙擊「啟動 Option Chaser.bat」
```

並確認：

- 不需先開 PowerShell
- 不需手動輸入指令
- 不需設定 PYTHONPATH
- 可以正常開啟瀏覽器
- Option Chaser 首頁正常
- 劇本工作區正常
- 關閉／停止流程清楚
- 第二次啟動不會產生多個混亂程序
- 錯誤時視窗不會瞬間消失
- Log 可以找到

---

# 16. 開發硬性原則

1. 不推倒已通過驗證的計算引擎。
2. 不把金融計算移到 GUI。
3. Artifact 是正式視覺方向，不是裝飾參考。
4. 不得再產生一張漂亮原型，卻不落實到 Repo。
5. 不得用「Streamlit 限制」作為不還原資訊架構的萬用藉口。
6. 若 Streamlit 無法合理達成 Artifact，必須在 Spec 階段坦白提出：
   - 可達成程度
   - 技術限制
   - 替代前端方案
   - 遷移成本
7. 候選價格是核心資訊，不可藏在原始報告。
8. 單腿與 Spread 必須有不同的產品表達。
9. 所有測試 fallback 必須明確標記。
10. 本版本完成標準是：

> 使用者雙擊 BAT 後，看見的實際 Option Chaser，就是 Artifact 中承諾的那個產品，而不是另一套工程後台。
