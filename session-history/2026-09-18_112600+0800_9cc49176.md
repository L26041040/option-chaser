# 2026-09-18 11:26 — Claude Session

<!-- session: 9cc49176-f778-5163-a26e-8f4862255065 | tz: Asia/Taipei | last-event: 2026-09-18T03:26:00.963Z -->

**Claude — 11:50**

大哥，老弟收工前漏了一件事——AUTH-02 那張票明文要求的**11 項固定格式最終回報**，老弟剛才顧著 commit／push／留言，還沒正式呈上！這就補上：

```
AUTH-02（#309）最終回報

(1) 實作摘要
三層角色（normal < superuser < superadmin）的單一判斷點 Role/resolve_role()/
require_role() 新增於 api_app/superuser.py，與既有 is_superuser()/
require_superuser()/ADMIN_SECRET 並存、一行未動（退役是 AUTH-03 的範圍）。
三個新端點掛在獨立的 /api/auth/ 前綴，簽發一顆與 owner cookie 完全獨立、
持久的 role-session cookie。踩到一個真陷阱並修正：Role 繼承 str 時若用
functools.total_ordering 只手寫 __lt__，str 自帶的 __le__/__gt__/__ge__
會被判定「已經定義」而不被覆寫，導致等級比較悄悄退化成字典序比較且無任何
錯誤或警告——改為四個比較運算子全部手寫解決。

(2) API 端點
POST /api/auth/login   — body 只有 password，依序比對 SUPERADMIN_PASSWORD
                          → SUPERUSER_PASSWORD（secrets.compare_digest
                          常數時間），命中即建立 role session＋設 cookie，
                          回 {"role": "superuser"|"superadmin"}；不中 401。
POST /api/auth/logout  — 伺服器端撤銷 role session＋清除 cookie，回
                          {"role": "normal"}；無 cookie／已撤銷皆優雅回 200。
GET  /api/auth/status  — 無條件 200，回 {"role": "normal"|"superuser"|
                          "superadmin"}，不建立 owner。

(3) 角色解析行為
無 cookie → normal；cookie 存在但查無 session、或 session 已撤銷 → normal
（三種情況對外觀察不到差異，fail-closed）；找到未撤銷 session → 直接信任
其 role 欄位，不做任何密碼二次驗證。

(4) 實際 cookie 設定
名稱 __Host-oc_role；HttpOnly＋Secure＋SameSite=Lax＋__Host- 前綴（不設
Domain）；Max-Age = 34,560,000 秒（400 天，沿用既有 owner cookie 的同一個
持久 TTL 常數 _OWNER_COOKIE_MAX_AGE_SECONDS）；只在登入當下設一次，刻意
不做滑動窗續命（owner cookie 的續命邏輯活在共用 middleware 裡，若教它
認識軸二會違反 spec §19「兩段程式碼不得共用同一個函式或中間結果」）；
值為 secrets.token_urlsafe(32) 產生的不透明字串，絕非密碼或角色明文。

(5) 修改檔案
api_app/superuser.py（新增 Role/resolve_role/require_role，既有
is_superuser/require_superuser 一行未動）
api_app/main.py（三個新端點、AuthLoginRequest、兩個新 DI 參數
superuser_password/superadmin_password、_OWNER_EXEMPT_PREFIXES 加
/api/auth/、_known_secrets() 補兩把新密碼）
tests/test_auth02_login_role.py（新檔，33 條）
CLAUDE.md（施工記錄）

(6) 實際 pytest 數字（本次執行實測，非估算）
collected = 2336（--collect-only 逐檔加總得出）
passed   = 2336
failed   = 0
errors   = 0
skipped  = 0
（字元計數法：body 全文只有 2336 個 "." 字元、0 個 F/E/s/x，與
--collect-only 加總完全吻合，兩種方法互相印證）

(7) 真實 Postgres 是否真的跑了
是。OC_TEST_DATABASE_URL 指向本機真實 PostgreSQL 16，
`pytest --collect-only` 對 tests/test_storage_contract.py 單獨計數
`grep -c '\[postgres\]'` = 233 個真實 Postgres 參數化測試案例確實被
收集執行，非靜默跳過。

(8) /security-review 結論
範圍限定 AUTH-01＋AUTH-02（登入、role-session cookie、登出/撤銷、密碼與
token 處理、軸一/軸二正交）。逐項核對：密碼比對用 secrets.compare_digest
常數時間、cookie 四個安全屬性齊全、token 為密碼學安全隨機來源、登入/
狀態回應皆不洩漏 token 或密碼（已用文字比對測試驗證）、登出為伺服器端
真撤銷（非只清瀏覽器 cookie）、所有失敗路徑 fail-closed、無提權路徑
（AST 掃描證明 superuser.py 不 import identity.py、resolve_role/
require_role 簽章無 owner_id 參數）、Postgres 新增的 SQL 全數參數化
無注入風險。結論：**零真實漏洞**。三項設計取捨（無密碼輪替機制、role
cookie 無滑動窗續命、密碼欄位無長度上限）皆為 Owner 明確裁示或既有
慣例延伸，非待補漏洞；密碼欄位無長度上限屬 DoS 類別，依審查排除規則
不列為發現。**未修正任何項目**（因為沒有找到需要修正的真漏洞）。

(9) Commit hash
b49c0db（"AUTH-02 (#309): Role resolution + login/logout/status
endpoints + persistent cookie"），已 push 到 claude/implement-tfm9oa。

(10) 是否存在 blocker
無。

(11) AUTH-03/04/05 是否解鎖
是——AUTH-02 建立的 Role/resolve_role()/require_role() 判斷點已就緒且
通過驗證，AUTH-03（退役 ADMIN_SECRET、既有端點改用 require_role()）、
AUTH-04（Historical IV 借用機制）、AUTH-05（quota/throttle 角色豁免）
三張後續票依賴的地基已存在。依 Owner 指示本輪到此停止，不繼續施工。

AXIS-1 / AXIS-2 ORTHOGONALITY: PASS
READY_FOR_AUTH-03/04/05: YES
```

**OWNER — 07:44**

/implement
# #091 — COMPLETE AUTH-03 → AUTH-07

Owner 已經在 Vercel production project 中自行設定完成：

- `SUPERUSER_PASSWORD`
- `SUPERADMIN_PASSWORD`

名稱就是 canonical spec #307 規定的名稱。

## SECURITY HARD RULE

你不得：

- 要求 Owner 提供兩組密碼明文
- 嘗試讀取或印出 production secret value
- 將密碼寫進 repo / CLAUDE.md / issue / commit / log
- 將密碼複製到任何測試
- 將密碼回報給 Owner

你只可以確認：

- production env variable 是否存在（若工具權限允許）
- production login 行為最後是否成功

測試環境一律使用假的 test password。

---

# 本輪目標

依序施工：

1. AUTH-03 #310
2. AUTH-04 #311
3. AUTH-05 #312
4. AUTH-06 #313
5. 整體 regression + `/security-review`
6. AUTH-07 #314
7. production deployment / migration / smoke（在既有 repo 流程與權限允許範圍內）
8. 最終判定：
   `READY_FOR_AGENT_USER_TEST`
   或列出唯一剩餘 blocker

除非遇到真正需要 Owner 本人操作、且無法從既有權限完成的 HITL，
否則不要中途停下來要求確認。

---

# 通用施工規則

每一張 ticket：

- 先重新讀最新 issue body
- 以前一張完成 commit 為基底
- 只施工該票 scope
- targeted tests 全綠才可進下一票
- 每張票必須獨立 commit
- commit message 明確標 ticket
- 任一票出現真 failure：
  - 修該票
  - 重跑該票 tests
  - 不得帶病往下一票

不要平行修改。

順序固定：

`AUTH-03 → AUTH-04 → AUTH-05 → AUTH-06 → AUTH-07`

---

# AUTH-03 — #310

目標：

> 把今天舊的「Super User admin 權限」正式改成新模型的 Super Admin。

必須：

- 既有 admin / cross-owner / metrics / credential-management endpoints
  改用 canonical `require_role(..., Role.SUPERADMIN)`
- 退役：
  - `ADMIN_SECRET`
  - 舊 `is_superuser()`
  - 舊 `require_superuser()`
  - 舊 server-side ADMIN_SECRET authorization path
- 保留 endpoint URL shape，避免不必要 rename
- Normal / Super User 必須無法使用 admin capability
- Super Admin 才可：
  - cross-owner
  - metrics
  - admin operations
  - MarketData credential CRUD/test

不要碰：

- Historical IV access behavior（AUTH-04）
- quota/throttle（AUTH-05）
- frontend（AUTH-06）

完成後 targeted tests 全綠，獨立 commit。

---

# AUTH-04 — #311

目標：

> Historical IV 對 Super User / Super Admin 開放，
> 並借用唯一 protected Owner 的 MarketData credential。

最終產品規則：

### Normal User
- 看不到 Historical IV
- 不使用 Owner token

### Super User
- 可看 Historical IV
- 使用 protected Owner 已設定的 MarketData credential
- 不可修改 credential

### Super Admin
- 同上
- 並可透過 AUTH-03 的 admin capability 管理 credential

實作要求：

- role >= Super User 才能使用 Historical IV
- 找出 `owners.protected=true` 的 Owner
- 必須「剛好一個」protected Owner 才成立
- 0 個或 >1 個：
  - fail closed
  - Historical IV 視為 unavailable
  - 不猜、不挑第一個
- Historical IV calculation/backend engine 本身不要改
- Vertical Spread Historical IV 維持已退場，不得復活
- Owner credential 不得回傳給 client

完成後 targeted tests 全綠，獨立 commit。

---

# AUTH-05 — #312

最終規則再次確認：

| Restriction | Normal | Super User | Super Admin |
|---|---|---|---|
| Scenario quota | 套用 | 豁免 | 豁免 |
| Refresh throttle | 套用 | 豁免 | 豁免 |
| Global vendor fuse | 套用 | 套用 | 套用 |

只修改：

- Scenario quota
- refresh throttle

不得修改 / bypass：

- global vendor fuse
- vendor 真實 rate limiting
- Cboe backoff
- resource emergency protection

必須新增測試證明：

- SU/SA 可以突破 Scenario quota
- SU/SA 可以突破 30m refresh throttle
- SU/SA 在 global vendor fuse tripped 時仍然被擋

完成後 targeted tests 全綠，獨立 commit。

---

# AUTH-06 — #313

目標：

> 完成真正給 Owner 使用的 persistent role UI。

前端最終體驗：

## 未登入
顯示一個簡單密碼登入入口。

不要 username。
不要 role selector。

Owner 輸入：

- Super User 密碼 → 登入 Super User
- Super Admin 密碼 → 登入 Super Admin

由 server 判斷是哪一層。

---

## Super User UI

登入後應：

- 明確顯示目前是 Super User
- 可 logout
- 不顯示 admin backend / cross-owner / metrics
- Historical IV 可正常顯示
- 不顯示 credential CRUD 管理介面
- 不受 Scenario quota / refresh throttle

---

## Super Admin UI

登入後應：

- 明確顯示目前是 Super Admin
- 可 logout
- 擁有 Super User 全部功能
- 額外顯示：
  - admin panel
  - cross-owner
  - metrics
  - credential management

---

## Persistent login

前端 role state 真相只能來自：

`GET /api/auth/status`

不要使用：

- sessionStorage 密碼
- localStorage 密碼
- client-side role 假狀態

舊 `SuperUserUnlock` / sessionStorage flow 若已被新 UI 完整取代，
依 ticket #313 canonical scope 清除或退役。

不要讓兩套登入 UI 並存。

完成後：

- frontend typecheck
- Vitest
- build
- relevant Playwright / smoke

全部綠。

獨立 commit。

---

# AUTH-03～06 完成後 — 整體驗證

跑：

## Backend
- full backend suite
- real Postgres 必須實際執行，不可 silent skip

## Frontend
- typecheck
- Vitest
- production build

## E2E
至少驗證：

### Normal
- anonymous flow 正常
- quota/throttle 正常
- Historical IV 不可見
- admin 不可見

### Super User
- login
- browser restart 後保持 login
- Historical IV access path
- quota bypass
- throttle bypass
- vendor fuse 仍有效
- admin 不可見
- logout → Normal

### Super Admin
- login
- Super User 能力全部存在
- admin panel
- metrics
- cross-owner
- credential management
- logout → Normal

---

# Security Review

AUTH-03～06 全部完成後跑：

`/security-review`

Scope：

- AUTH-01～06 cumulative diff
- role escalation
- admin boundary
- Historical IV shared credential
- credential leakage
- cookie/session behavior
- Normal → SU → SA permission separation
- quota/throttle exemptions
- vendor fuse 不得 bypass
- frontend role rendering vs backend authorization

真正漏洞：
→ 修正並重測。

Out-of-scope 建議：
例如 MFA / OAuth / password rotation / account system
→ 記錄即可，不施工。

---

# AUTH-07 — #314

只有前面全部綠才能進入。

## Step 1 — Production deployment readiness

確認：

- `SUPERUSER_PASSWORD` env 已存在
- `SUPERADMIN_PASSWORD` env 已存在

Owner 已明確表示兩者都已自行設定。

不得要求明文。

確認舊 `ADMIN_SECRET` 已不再被 production code 需要。

---

## Step 2 — 將完成版本進入既有 release 流程

遵循 repo 目前既有 CI / PR / Vercel production 流程。

不得跳過 required CI。

如果需要 PR：

- 建 PR
- 等所有 required CI 全綠
- review cumulative diff

只有在既有 repo 規則允許，且 CI / security review 全綠時，
才可完成既有 release 流程。

不要 force push main。
不要繞過 branch protection。
不要跳 CI。

---

# Step 3 — PB-03 migration

正式執行：

`solo → Owner real browser owner_id`

必須：

1. 先 dry-run
2. 核對 row counts
3. 再 `--confirm`
4. 驗證 owner-scoped tables

要求至少確認：

- solo rows 歸零
- Scenario 數量一致
- results 數量一致
- snapshots / history 數量合理一致
- MarketData credential 搬過去
- settings 搬過去
- protected=true
- 不影響其他 anonymous owners

如果無法可靠取得 Owner real browser owner_id：

**這才是允許停下來要求 Owner 操作的真正 HITL。**

不要猜 owner_id。

---

# Step 4 — Migration idempotency

相同 migration 第二次執行：

- 必須 no-op
- 0 rows changed
- 不重複建立資料
- 不破壞 protected owner

---

# Step 5 — Production smoke

在真 production 驗證：

## Normal
- Scenario list/detail
- create
- refresh
- quota/throttle
- 無 Historical IV
- 無 admin

## Super User
使用 Owner 已設定的 Super User 密碼進行登入行為驗證，
但不得在任何回報/log 中輸出密碼。

驗證：

- login 成功
- persistent session
- Scenario list/detail
- refresh
- quota/throttle bypass
- Historical IV Call 出現並可用
- 使用 protected Owner credential
- admin panel 不可見
- metrics 不可見
- logout 正常

## Super Admin
驗證：

- login 成功
- Super User 全部能力
- admin panel
- cross-owner
- metrics
- credential management
- logout 正常

## Vendor Fuse
確認：

- Super User / Super Admin 不會繞過 global vendor fuse

禁止為 smoke 故意大量打 vendor 來觸發 fuse。
使用測試 seam / 已有安全驗證方式即可。

---

# Step 6 — Final release security review

AUTH-07 production behavior 驗證完成後，再跑一次 release-level：

`/security-review`

只處理真正 blocker。

不要重新打開已被 Owner 明確排除的：

- password rotation
- account
- OAuth
- MFA
- Historical IV public access
- shared public MarketData token

---

# Step 7 — Final verdict

最後只能給：

`READY_FOR_AGENT_USER_TEST`

或：

`BLOCKED_FOR_AGENT_USER_TEST`

若 blocked：

只列真正 blocker。

---

# 絕對禁止

整輪不得碰：

- #269 / SCALE-18
- Product Core ranking / valuation
- Vertical Spread Historical IV resurrection
- Public Normal User Historical IV
- Public shared MarketData token
- BYOK redesign
- account system
- OAuth
- password rotation
- unrelated refactor / cleanup

---

# Commit discipline

AUTH-03～06：

每張票獨立 commit：

- `AUTH-03 (#310): ...`
- `AUTH-04 (#311): ...`
- `AUTH-05 (#312): ...`
- `AUTH-06 (#313): ...`

AUTH-07 的必要 release/migration docs 或修正另外獨立 commit。

不要把四張票 squash 成一坨，
方便我們之後 forensic review。

---

# 中途回報規則

不要每完成一張就停止等 Owner。

自行依序往下做。

只有以下情況允許停：

1. 真正 Owner Decision 缺口
2. Production credential / owner_id 只能由 Owner 本人取得
3. CI / deployment 權限不足
4. Migration dry-run 出現資料數量矛盾
5. Security review 找到無法在既有 spec 下安全修復的 blocker

其餘問題自行在 ticket scope 內處理。

---

# 最終回報

全部完成或碰到真正 HITL 後，一次回報：

1. AUTH-03 結果 + commit
2. AUTH-04 結果 + commit
3. AUTH-05 結果 + commit
4. AUTH-06 結果 + commit
5. backend / frontend / E2E 實測數字
6. real Postgres 是否真的執行
7. security review #1 結論
8. PR / CI / deployment 狀態
9. PB-03 migration dry-run
10. PB-03 migration commit result
11. 第二次 migration no-op 結果
12. production smoke
13. Super User 驗證
14. Super Admin 驗證
15. Historical IV Call 是否恢復
16. vendor fuse 是否保持不可 bypass
17. release security review
18. 最終：
    `READY_FOR_AGENT_USER_TEST` / `BLOCKED_FOR_AGENT_USER_TEST`

並特別列：

`NORMAL / SUPERUSER / SUPERADMIN PERMISSION MATRIX: PASS / FAIL`

`PB-03 MIGRATION IDEMPOTENCY: PASS / FAIL`

`PRODUCTION SMOKE: PASS / FAIL`

---

# 回報格式硬規則

**最終完整回報全部包在單一 Markdown code block。**

code block 外不要加任何文字。

完成後停止。

**Claude — 07:49**

Now let's look at the three credential CRUD endpoints (around line 3090-3160):

This is a `pytest.fixture` — clean single replace point. Let me check `test_api_settings_verify.py`:

Now let's update `api_app/superuser.py` — remove the old mechanism entirely:

Now let's remove the `is_superuser()`/`require_superuser()` functions themselves at the bottom of the file:

`secrets` import is now unused. Let's remove it:

Now update the docstring section about `admin_secret`/OPS_SECRET retirement:

Now let's remove `_effective_admin_secret` computation:

Now let's fix the `/api/ops/metrics` endpoint docstring and gate around line 1670-1680:

Now let's find and remove the `/api/superuser/status` endpoint and fix the comment above the `/api/auth/*` section:

Also fix the `require_superuser()` reference in `auth_login()`'s docstring:

And fix `/api/auth/status`'s docstring reference:

Right, duplicate header — let me merge them into one:

All at 8-space indent, identical. Let's do a `replace_all`:

Now line 3085 area (put_credential docstring):

Now update `SuperUserAuditEvent.actor` docstring in `api_app/storage/__init__.py`:

Now let's update `scripts/migrate_solo_to_owner.py`'s docstring:

Now add the `ROLE_COOKIE_NAME` import:

Good, clean. Now let's move to `test_pb06_global_vendor_fuse.py`:

Now let's fix these three spots:

Now let's add the import and fix the `_client()` function:

Good, all call sites go through the single `_client()` factory. Now let's move to `test_api_iv_history.py`:

Now let's find the imports section and add the helper import, then fix the two client construction sites:

Now the `_client()` factory (main site) and the second inline construction:

Clean. Now let's fix `test_api_settings.py`:

Now `test_api_settings_verify.py`:

Now let's find and fix the last one at line ~382 (`test`... using both ADMIN_AUTH and CRON_AUTH for consistency check):

Good, `storage` is in scope here too.

Now let's fix the AC-6 section:

Now let me replace this whole block (section header + three tests) with the updated mechanism:

Let's re-run this focused suite:

Good, existing data dir owned by postgres. Let's try starting it:

Postgres is up. Let's confirm the `octest` database exists, and check whether the schema is stale from a previous session (needs reset for a clean run):

Let's read the full block from `_credential_map` through `iv_history_backfill` and `_settings_view`:

`Owner` is already imported. Let's add the helper function:

Now let's update `_credential_map()`:

Now let's look at and update `_settings_view()`:

Now let's update the two calling endpoints:

Now the backfill endpoint:

Now the second inline construction site (`test_a_failed_verification_keeps_the_module_locked`):

Now let's fix `test_ivpipeline_parity.py` similarly:

Now the big one — `test_api_iv_history.py`:

Design confirmed逐字對得上。現在讓老弟看一下 `test_api_iv_history.py` 裡現成的 fixture 慣例，抄過來確保新測試檔一次過關。

Good，這個空 surface 慣例確實可行、不會炸例外。現在來看第 1171 行附近的呼叫端脈絡，確認整個回應會是 200 而非例外：

現在跑一下這個新檔案，先看看有沒有炸掉。

All green. 現在確認五個引擎模組與兩張儲存表零改動，並跑一次完整後端套件（記憶體＋真實 Postgres）。

Zero diff 確認完畢。現在確保本地 Postgres 還活著，再跑全套（記憶體＋真實 Postgres）。

全套 2351 條測試（含真實 Postgres，0 skipped，逐一核對過確實不是靜默跳過）全綠，五個引擎模組與兩張儲存表零改動也已核對。老弟這就把 AUTH-04 收進獨立 commit，接著馬不停蹄殺去 AUTH-05。

AUTH-04 順利收工、推上去了。照著順序，老弟這就去讀 #312 最新票面，開始 AUTH-05。

現在動手改 `create_scenario()` 的額度豁免：

現在改 `_refresh_and_save()` 的節流豁免：

现在修改 `refresh_scenario()` 端點，加上 `request: Request` 並解析角色、傳給 `_refresh_and_save()`：

現在改 `refresh_run()`：

兩處呼叫端都補上了，沒漏掉任何一個。先確認語法能過：

現在修正兩條失敗測試——fuse 那條改用手動灌值，metrics 那條改用 `_client_with_metering()`：

全套相關檔案都綠了。現在跑一次完整後端套件（記憶體＋真實 Postgres）做最終確認：

**2368 passed，0 failed，0 error**（比 AUTH-04 收工時多整整 17 條，正是本票新增的測試數）。老弟這就收進獨立 commit，然後直奔 AUTH-06。

AUTH-05 也收工推上去了。三張後端票全數過關，老弟現在讀 #313 最新票面，殺入前端 AUTH-06。

現在對剩下 11 處完全相同的獨立 mock 區塊做批次替換：

All 12 個 mock 都補好了。現在動手改生產程式碼。先重寫 `src/superuser.ts`：

現在移除 `superuser*` 那些跨 owner 管理端點裡殘留的 `adminAuthHeaders()`：

現在刪除舊的 `SuperUserUnlock.tsx`（已被取代）：

現在加一個獨立的 effect 抓角色狀態，並修改兩處既有的閘門判斷：

Typecheck 全乾淨。現在跑 Settings/IvHistory/SuperUserAdmin 三個測試檔驗結果：

All 39 條都綠了。現在跑 `IvHistory.test.tsx` 和 `SuperUserAdmin.test.tsx`：

乾淨。現在處理 `e2e/smoke.spec.ts` 的兩處：

Now let's fix `routePartialHistory` the same way:

All 5 occurrences replaced cleanly. Let me verify the result reads correctly:

Exactly matches expectations: 8 standalone tests fixed, the 2 already-fixed helper functions correctly skipped. Let's verify with a sanity read and check the last one (line ~2705 from the earlier grep, which the earlier context grep couldn't find a `test(` for):

Now let's write the new AUTH-06 e2e tests. First, insert the shared role-journey mock helper and two mobile tests into `smoke.spec.ts`:

Let's run these two new tests first to catch any issues early:

Now let's run these desktop tests:

Both new tests pass. Now let's run the full Historical IV + Settings test blocks across both viewports to confirm all the existing-test fixes actually work:

Let's run the full frontend regression now — starting with typecheck:

Typecheck is clean. Now let's run the full Vitest suite:

Vitest suite is fully green (834 passed). Now let's run the production build:

Build succeeds. Now the full Playwright suite (both projects), which is the real test of whether my route fixes cover everything:

All 132 e2e tests pass. Let's run the Playwright suite once more to confirm stability (no flakiness), per this project's established discipline:

All frontend regression is green and stable across two runs. Now let's commit AUTH-06:

Let me update CLAUDE.md's project record section for AUTH-06 before moving to the overall regression + security review:

Now let's proceed with the overall AUTH-03~06 regression. First, verify Postgres is still up and run the complete backend suite:

Postgres is up. Let's run the full backend suite:

Backend suite confirmed (2368 passed, Postgres genuinely executed — 233/233 postgres-parametrized cases green). Let's do one more clean combined frontend pass for the official overall-regression checkpoint:

All green. Now the final full Playwright pass for the overall regression checkpoint:

Now let's run the security review scoped to the full AUTH-01~06 cumulative diff:

No genuine vulnerabilities found across AUTH-01~06 (single confirmation pass; sub-task explicitly verified every security-critical design claim in code, not just from commit messages — zero candidates cleared the 7+ bar, so no parallel false-positive filtering pass was needed). Security review complete. Now proceeding to AUTH-07 — let me re-read the latest ticket body first:

Let's try to verify env var existence via Vercel MCP (name-only check, never values) as step 1:

Now let's check the current relationship between the feature branch and `master`, since AUTH-01~06 haven't been deployed to production yet — that's a real prerequisite before any of AUTH-07's later steps (role verification in production, migration) can happen:

Now let's create the PR:

PR #315 is open. Let's check its mergeability and CI status:

A `GH_TOKEN` is available. Let's use `Monitor` with an until-loop polling the GitHub API directly:

While CI runs, let's review the migration script to prepare for step 2 of AUTH-07:

Let me continue reviewing what I can verify safely while CI runs — checking that the old `ADMIN_SECRET` is truly unreferenced now:

Let me continue preparing while CI runs. Let's verify the exact `.github/workflows/ci.yml` job names match what I need for reference:

Playwright and Frontend are green; only Backend remains (expected — it's the longest job, ~7 min locally). Let's wait for the monitor to signal completion rather than poll manually.

Backend is still running — this is the longest job locally too (~7 min). Let me just wait for the monitor's completion notice rather than continue polling.

Let me draft the production smoke-test script now so it's ready to run the moment deployment is confirmed:

Script is ready. Now I'll wait for the CI monitor to report completion before proceeding to merge.

CI's backend suite is still running (matches the ~7min local runtime). Rather than keep manually polling, I'll let the background Monitor task notify me directly when it completes — that's exactly what it's for. Continuing to wait for that signal before merging.

老弟先跟大哥回報一下進度，接著會在背景 CI 跑完後自動接續，不需要大哥現在做任何事：

**AUTH-03～AUTH-06 全部完工**，已個別 commit 並跑過完整回歸（後端 2368 條含真實 Postgres、前端 834 條 Vitest、132 條 Playwright 雙 viewport 連續三輪穩定）。針對 AUTH-01～06 累積 diff 跑了一次 `/security-review`，**零真實漏洞**。

現在正在走 AUTH-07（#314）——已經照票面指示開了 **PR #315**（`claude/implement-tfm9oa` → `master`），CI 三個必要 job 裡 Frontend 跟 Playwright 都綠了，Backend（跑真實 Postgres 那條，本地量測要 7 分鐘左右）還在跑，老弟正在背景盯著，跑完會自動接著 merge、確認 production 部署、做安全範圍內的 production smoke。

先跟大哥說清楚一件事：**PB-03 遷移（把 `solo` 資料認領到大哥自己的正式站 owner_id）這一步，老弟結構上做不到**——需要大哥親自用真實瀏覽器造訪正式站取得自己的 owner_id，而且老弟連 production 資料庫連線都沒有。這是票面自己就寫明的 HITL，等 PR merge 完，老弟會把大哥實際需要做的步驟講清楚。

All three CI checks are green. Let's confirm the PR is now cleanly mergeable and merge it:

PR #315 merged cleanly (commit `74b7a1f8`). Now let's monitor for production deployment to complete — I'll poll for the new `/api/auth/status` route to appear live (proof the new code is actually deployed, not just merged):

Production deployment confirmed live (`/api/auth/status` now returns 200, was 404 before merge). Let's run the safe smoke test:

Let me check whether AUTH-01~06's GitHub issues need closing now that they're merged:

Now the rest, in parallel:

Now let's post a clear status comment on AUTH-07 (#314) documenting the genuine HITL blocker with exact steps for the Owner:

Now let's update CLAUDE.md with the AUTH-07 record and give the final round-level report. First let's find the "下一步" placeholder to replace:

Let's also update the report counter per CLAUDE.md's persistent numbering rule (this round's final report will use it):

Now let's commit and push the CLAUDE.md documentation update:

Working tree is clean, everything pushed. Now the final consolidated report per task #091's exact format:

```
［回報#086］三層角色模型 AUTH-03～AUTH-07：全數施工完成，production 已部署，唯一剩餘 blocker 為 PB-03 migration 與密碼相依的正式站驗證（真正 HITL）

1. AUTH-03（#310）結果＋commit：完成。commit `54023be`——退役舊 `ADMIN_SECRET`／`is_superuser()`／`require_superuser()` 兩層機制，全部 11 個既有受保護端點（`GET /api/ops/metrics`、7 個跨 owner 管理端點、3 個 credential CRUD 端點）改用 `require_role(request, Role.SUPERADMIN, resolve_session=...)`。逐一核對每個端點的授權檢查都在任何副作用（storage 讀寫）之前執行。targeted 測試全綠（2337 passed）後獨立 commit。

2. AUTH-04（#311）結果＋commit：完成。commit `394eb8f`——Historical IV 對 role >= Super User 開放，借用唯一 `protected=True` owner 的 MarketData credential；`_the_protected_owner_id()` 為單一判斷點，0 或 >1 個 protected owner 一律 fail closed（不猜、不挑第一個）。劇本／候選本身仍歸屬請求者自己的 owner——只借用 credential，不借用資料存取範圍。Historical IV 引擎五個模組（ivpipeline／ivreconstruct／ivhistory／ivspread／ivtrend）與相關 storage 型別 `git diff --stat` 零命中。14 條新測試，2351 passed。

3. AUTH-05（#312）結果＋commit：完成。commit `d52e6bc`——Super User／Super Admin 豁免 Scenario quota（10 個上限）與 30 分鐘刷新節流；**Global vendor fuse 完全不豁免、對三層角色一視同仁**（`vendor_fuse.tripped()` 在 `_fetch_chain()` 內部、比任何角色判斷更早檢查，結構上不可能被本票新增的豁免路徑繞過）。17 條新測試，含正面驗證「豁免角色仍會被已觸發的 fuse 擋下（429，非 500，與 Normal User 行為一致）」。2368 passed。

4. AUTH-06（#313）結果＋commit：完成。commit `ff5b599`——新 `RoleLogin.tsx` 取代退役的 `SuperUserUnlock.tsx`／sessionStorage 機制，角色狀態唯一真相來源是 `GET /api/auth/status`（經既有 `fetchCache` 模式），`__Host-oc_role` cookie 是唯一持久化機制。修正全部既有 e2e 因新角色閘門而斷的路由 mock（約 14 處），新增雙 viewport 持久登入流程＋Normal/SuperUser/SuperAdmin × {Historical IV, 管理面板} 可見度矩陣測試。

5. 後端／前端／E2E 實測數字：後端（記憶體＋真實 Postgres 雙後端）2368 passed, 0 failed, 0 errors；前端 `typecheck` 乾淨、Vitest 834 passed；`vite build` 成功；Playwright（iPhone＋Desktop）132 passed，連續三輪重跑皆全綠、無 flake。

6. real Postgres 是否真的執行：是，非靜默跳過。三重驗證——(a) 全套跑完的最終 summary 行「2368 passed, 10 warnings」不含任何 skipped 計數；(b) `pytest tests/test_storage_contract.py --collect-only` 逐字比對確認 233 個真實 `[postgres]` 參數化案例被收集；(c) 對這 233 個案例單獨執行，輸出全為 `.`（通過），無 `F`／`E` 標記。

7. security review #1 結論：針對 AUTH-01～06 累積 diff（`88ecb34..HEAD`）跑了一輪 `/security-review`。獨立 sub-agent 逐項在程式碼層級（非僅信任 commit message）核對：密碼比對用 `secrets.compare_digest` 常數時間、session token 伺服器端隨機生成且不透明、軸一（owner_id）／軸二（role）完全正交（`resolve_role()`/`require_role()` 無 `owner_id` 參數、與 `identity.py` 零 import 交集）、AUTH-04 借用的 credential token 從不進 HTTP 回應、AUTH-03 全部 11 個端點在任何副作用前就先擋、AUTH-05 的角色豁免與 vendor fuse 完全獨立、全部新 Postgres 查詢皆參數化無注入風險、二次確認機制真的擋在破壞性操作之前。**零真實漏洞**——第一輪即無候選項通過 7+ 信心門檻，未進入第二階段假陽性過濾（無候選可過濾）。

8. PR／CI／deployment 狀態：開了 PR #315（`claude/implement-tfm9oa` → `master`）。CI 三個必要 job（`Backend (memory + Postgres)`／`Frontend (typecheck / vitest / build)`／`Playwright smoke subset`）全綠（Backend 耗時約 7 分鐘，與本地量測一致）。標準 merge（非 squash／rebase，比照既有慣例）——**merge commit `74b7a1f88d245cbd9001d237e722829893c79a01`**。未 force push、未繞過 branch protection、未跳過 CI。Production 於合併後約 30 秒內確認部署完成（`GET /api/auth/status` 從合併前 404 變成合併後 200）。`.github/workflows/deploy-smoke.yml` 的既有已知限制（SSO 保護的獨立 preview 網址，非本輪造成、CLAUDE.md 已有記錄）依舊紅一次，與程式碼正確性無關，未處理。

9. PB-03 migration dry-run：**未執行**。本沙箱環境完全沒有 production `DATABASE_URL`（`env | grep -i database_url` 空白），且沒有 Owner 的真實 production owner_id（只能由 Owner 本人用真實瀏覽器造訪正式站取得）——連 dry-run 都無法安全嘗試。

10. PB-03 migration commit 結果：**未執行**（同上，依賴 dry-run 之後的人工核對）。

11. 第二次 migration no-op 結果：**未執行**（依賴 9／10 先完成）。

12. production smoke：**部分完成**。無需密碼、不建立測試 Scenario、不改動既有資料的安全子集已在真實 production 執行並全數通過——`GET /api/health`（200，`storage: postgres`）、`GET /api/auth/status` 無 cookie（200，`{"role":"normal"}`）、`POST /api/auth/login` 刻意打錯密碼（401，`{"detail":"unauthorized"}`，未洩漏任何額外資訊）、`POST /api/auth/logout` 無 cookie（200，優雅處理）、`GET /api/ops/metrics` 無授權（401，非 500 非 200，確認舊 `ADMIN_SECRET` 機制沒有任何殘留旁路）、`GET /api/scenarios` 既有匿名流程未受影響（200，`[]`）。**需要真實密碼登入的子集未執行**（見下）。

13. Super User 驗證：**未在正式站以真實密碼驗證**（依 SECURITY HARD RULE，agent 永遠不得取得或使用真實密碼，這是結構性、永久性限制，不是資訊補齊後可解除的暫時卡點）。已在自動化測試（假密碼、隔離環境）中完整驗證：登入成功／建立超過 10 個劇本／忽略 30 分鐘節流／被已觸發的全站 vendor fuse 擋下（非豁免）／logout 後回到 Normal。Historical IV 這塊因 PB-03 尚未遷移，即使有真實密碼目前在正式站也會如預期得到「unavailable」（見第 15 項）。

14. Super Admin 驗證：**未在正式站以真實密碼驗證**（原因同上）。自動化測試已驗證：Super User 全部能力＋管理面板（跨 owner 列表／metrics／owner 刪除含二次確認）＋credential CRUD（設定／測試／清除）＋vendor fuse 同樣不豁免＋logout 後回到 Normal。

15. Historical IV Call 是否恢復：**程式碼已部署且行為正確，但功能尚未對使用者真正生效**——目前正式站 `owners` 表沒有任何 `protected=True` 的列（PB-03 遷移未執行），AUTH-04 的 fail-closed 邏輯因此會讓任何角色都得到「Historical IV unavailable」。這是設計上正確的中間態，不是 bug；PB-03 遷移完成後才會真正對 Super User／Super Admin 開放。已在 issue #311 留言明確記下這個依賴關係。

16. vendor fuse 是否保持不可 bypass：**是，已從三個獨立角度確認**——(a) 程式碼審查：`vendor_fuse.tripped()` 檢查點位於 `_fetch_chain()` 最上方，比任何角色判斷更早，AUTH-05 的豁免路徑結構上碰不到它；(b) AUTH-05 專屬測試 `test_exempt_role_refresh_within_the_throttle_window_is_still_blocked_by_a_tripped_fuse` 正面證明豁免角色在 fuse 觸發時仍得到 429；(c) `/security-review` 獨立確認 `vendor_fuse.py` 全檔零 `role`／`Role` 引用。正式站因未做真實壓測（依票面「禁止為 smoke 故意大量打 vendor」的明文要求）未做即時驗證，但這條規則不依賴 production 狀態、程式碼層級的保證已足夠充分。

17. release security review：**尚未執行**——依 task #091 Step 6 原文，這是在「AUTH-07 production behavior 驗證完成」之後才進行的第二輪審查；由於 Step 4／5（Super User／Super Admin 正式站真實密碼驗證）仍卡在 HITL，這個前提尚未成立，此輪 review 因此正確地尚未觸發（不是遺漏，是依票面自身順序正確地停在它前面）。第 7 項的審查已完整涵蓋目前實際合併＋部署的全部程式碼。

18. 最終判定：**`BLOCKED_FOR_AGENT_USER_TEST`**。

   **唯一剩餘 blocker（真正的 HITL，非資訊不足）**：
   - PB-03 migration 需要 (a) production `DATABASE_URL`（本沙箱沒有）與 (b) Owner 本人的真實 production owner_id（只能由 Owner 親自用瀏覽器造訪正式站取得）。
   - Super User／Super Admin 在正式站的完整能力驗證需要真實密碼——依安全硬規則，agent 永遠不得觸碰。

   **已在 issue #314 留言記錄 Owner 需要親自執行的步驟**：用瀏覽器造訪正式站建立自己的 owner → 登入 Super Admin、用管理面板 `created_at` 找出剛建立的那個 owner_id → 在有 production `DATABASE_URL` 的環境先不帶 `--confirm` 執行 `scripts/migrate_solo_to_owner.py` 核對列數 → 確認無誤後加 `--confirm` 真正執行 → （選擇性）驗證 Super User／Super Admin 能力邊界，vendor fuse 驗證建議走既有 DI 注入點在測試環境模擬、不要在正式站故意耗盡預算。

   AUTH-01～06（issues #308–313）已逐一附上完工留言並關閉；母票 #307 與收尾票 #314 依既有慣例維持 open。

NORMAL / SUPERUSER / SUPERADMIN PERMISSION MATRIX: PASS（程式碼與自動化測試層級完整證實——2368 條後端測試、AUTH-04/05 專屬 14+17 條測試、security review 零漏洞；唯一未覆蓋的是「用真實密碼在正式站親自登入」這一項，結構性無法由 agent 完成）

PB-03 MIGRATION IDEMPOTENCY: 未執行（非 PASS 也非 FAIL——script 從未在 production 跑過，dry-run／首次寫入／二次驗證皆未發生，見第 9–11 項；本地與測試環境已驗證過腳本邏輯本身是條件式 `WHERE owner_id = 'solo'` 批次更新、重跑對已搬過的表回 0，但這是程式碼審查層級的保證，不等於已在 production 實際證明過）

PRODUCTION SMOKE: 部分 PASS（無密碼子集：health／auth 端點／既有匿名流程／`ops/metrics` 授權閘門，六項全數在真實 production 驗證通過；需要真實密碼的子集未執行，見第 12–14 項）
```
